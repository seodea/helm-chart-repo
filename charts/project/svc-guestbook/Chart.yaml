apiVersion: v1
name: svc-guestbook
version: 1.0.0 

dependencies:
  - name: subchart-test # Parent chart name 이름 
    version: 0.1.0 # Parent chart version 기입
    # repository: "https://gitlab.mzcloud.xyz/api/v4/projects/328/packages/helm/sdh" # Priavte parent chart URL 사용 
    repository: "https://seodea.github.io/helm-package/charts/main/" # Argo CD에서 테스트를 위해서 Public parent Chart URL 사용
    # repository: "@github-helm" # parent chart path (Argo CD repositories)
    # repository: "file://../main/" # parent chart path